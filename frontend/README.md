# 个人知识库智能助手 - 前端

React + TypeScript + Vite 构建的前端应用。

## 功能特性

- 💬 **智能对话**：基于 RAG 和用户记忆的个性化对话
- 📚 **知识库管理**：添加和管理文档到向量知识库
- 🧠 **记忆管理**：查看和管理用户记忆

## 快速开始

### 安装依赖

```bash
cd frontend
npm install
```

### 开发模式

```bash
npm run dev
```

应用将在 http://localhost:3000 启动。

### 构建生产版本

```bash
npm run build
```

## 项目结构

```
frontend/
├── src/
│   ├── api/          # API 客户端
│   ├── components/   # React 组件
│   │   ├── Chat.tsx
│   │   ├── Documents.tsx
│   │   └── Memories.tsx
│   ├── App.tsx       # 主应用组件
│   └── main.tsx      # 入口文件
├── package.json
└── vite.config.ts
```

## API 配置

默认连接到 `http://localhost:8000`，可以通过环境变量修改：

创建 `.env` 文件：
```
VITE_API_URL=http://localhost:8000
```

